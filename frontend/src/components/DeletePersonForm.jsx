const DeletePersonForm = ({ rowObject, backendURL }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeletePersonForm;